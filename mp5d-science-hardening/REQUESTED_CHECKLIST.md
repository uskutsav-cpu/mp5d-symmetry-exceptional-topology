1. **Fix the EP evidence framework before running anything expensive.** This is the highest-priority issue. Your current authoritative status correctly withdraws the old `|a₁/a₂| = 0.3107` quantity because the continued fraction is meromorphic, and it also withdraws the discriminant as an independent statistic because it is simply the squared frequency gap.  Remove both from every active scientific gate and do not let future code use them as independent EP evidence. Redesign the EP2 gate around quantities that actually test defectiveness: independently located coalescing frequencies, eigenvector/geometric-multiplicity collapse, monodromy exchange, square-root/Puiseux splitting under parameter perturbation, Jordan/generalized-eigenvector evidence where numerically meaningful, and agreement across genuinely independent solvers. For argument-principle work, do not use the meromorphic CF blindly; use an analytic/regularized spectral function whose poles are controlled, ideally from the recurrence-free formulation. 
2. **Turn the ten rejected candidates into permanent regression tests.** You already found two serious branch-tracking bugs in the history, so the next code should deliberately try to reproduce those failures. Add something like `tests/continuation/test_branch_tracking_adversarial.py` that tests forward continuation, reverse continuation, step-halving, continuation from independent seeds, loop closure in parameter space, branch identity through near-crossings, and explicitly reproduces the old label-collapse and branch-jump cases and proves the corrected tracker does not fail there. For each of the ten closest interactions, store the expected two branches and verify that increasing precision/resolution keeps them distinct. 
3. **Do much stronger convergence ladders on the strongest interactions.** Do not rely on one resolution. For approximately the 10–20 most dangerous points, rerun Solver A and Solver C through radial-resolution ladders, angular-resolution ladders, CF-depth ladders, working-precision ladders, Solver-C contour-length ladders, complex-scaling-angle ladders, and continuation-step-size ladders. Record actual convergence curves. The final claim should survive all of them. Your broad atlas can stay relatively economical; the dangerous points should be over-tested. 
4. **Stress-test the boundary where your minimum occurs.** Your bounded result currently reaches 50,558 atlas points and has its smallest gap near the boundary of the searched domain.  Build an adaptive-refinement routine around the minimum rather than only a uniform grid. Start from the current tightest location, refine `s,\delta,\mu`, then repeat until the estimated minimum stops moving materially. Also approach that region from several continuation directions. This is much more persuasive than saying “the grid had 50k points.” 
5. **Decide whether you want the current bounded paper or the strongest possible paper.** For the existing bounded claim, you do **not** have to solve every unsearched regime—you can clearly declare them outside the domain. If you want to push the project substantially further before freezing it, the biggest remaining scientific task is the **quasiresonant regime**. Your current final gate explicitly says radial certification remains partial and that a hyperboloidal formulation is the next scientific method because the existing recurrence-free solvers fail as the quasiresonant limit is approached.  In that stronger version I would implement **Solver F: hyperboloidal QNM solver**, validate it first on ordinary modes and published benchmarks, cross-check it against A/C inside their valid domains, and only then use it where A/C break down. That is the one major new solver I think is scientifically worth coding. A Wronskian/matched-asymptotic solver would be a secondary fallback, not my first priority. 
6. **Extend the search slightly in mode number as a robustness check.** Your formal result can remain bounded at `N=0\ldots3` and the stated `l`-range, but before freezing I would do targeted extensions to at least `N=4,5` and the next one or two `l` branches in the sectors that produced the smallest gaps. You do not necessarily need another 50,000-point atlas. A coarse scan followed by adaptive refinement is enough to see whether an entirely new interaction family suddenly appears. If nothing dangerous appears, state that as an out-of-domain robustness probe rather than incorporating it into the formal exclusion box. 
7. **Strengthen radial certification instead of leaving the DFT issue vague.** This may be the best technical improvement after branch tracking. Your current certification is partial because recurrence coefficients come from numerical DFT recovery without a rigorous aliasing bound.  I would try to eliminate that weakness by deriving the transformed radial polynomial/recurrence coefficients symbolically and evaluating them directly, rather than recovering them through FFT samples. If the degree really is known exactly after clearing denominators, encode and test that degree bound. Then use `python-flint`/Arb to ball-evaluate the coefficients. After that, implement interval Newton or Krawczyk certification for the **finite algebraic/truncated problem**. Be careful with the wording: even a rigorous matrix/recurrence certificate is not yet a continuum QNM theorem. 
8. **Create a real submission-science CI pipeline.** Your ordinary CI currently excludes slow tests, and the manual research workflow has not been run. The existing manual workflow also contains an escape hatch around the baseline script.   Change this so there is one command that fails on **any** scientific failure, for example `./scripts/reproduce_submission_science.sh`. It should install the locked environment, run all tests including slow tests, reproduce analytic artifacts, published benchmarks, atlas manifests, candidate checks, multisolver validation, certification checks, bibliography metadata checks, and final status/provenance checks. No `|| echo`, no “warning but continue” for submission-critical tasks. Then run that workflow on GitHub Actions from the exact commit you plan to freeze. 
9. **Add automatic provenance and claim-consistency tests.** I would code at least `scripts/check_provenance.py`, `scripts/check_result_schema.py`, and `scripts/check_claim_consistency.py`. Every authoritative result should carry the exact generating commit, environment hash, solver/version, precision, resolution, residuals, and independent-check information. The CI should fail if a final JSON points to an ancestor instead of the science-freeze commit. The claim checker should also fail if a diagnostic marked `WITHDRAWN` appears in an active result summary. This would have automatically caught the README/status mismatch we found. 
10. **Verify the literature inputs and numerical benchmarks one final time.** For every published number you use to validate your solvers, independently re-check the table, parameter conventions, angular labels, horizon normalization, frequency convention, and units. You already caught one table-transcription/convention problem earlier, so this deserves a dedicated regression manifest. Generate benchmark data from a machine-readable provenance file and test it rather than hand-copying numbers into test functions. 
11. **Use Lean only for the exact mathematical layer.** I would create a separate `lean/` project using current `mathlib`, and I would formalize roughly five things. First, the **direct-sum generalized-eigenspace/Jordan structure theorem**: block-diagonal invariant sectors cannot manufacture a generalized eigenvector chain by mixing blocks. This is the algebraic core of the cross-sector no-go. Be very precise about whether you are proving it for ordinary linear operators, matrix pencils, or the exact operator family used in the paper; a finite-matrix theorem must not be advertised as a continuum-QNM theorem. Second, formalize the **sector-invariance consequence** abstractly: if an operator commutes with the two commuting symmetry actions/projectors, the corresponding Fourier/isotypic subspaces are invariant. You probably do not want to formalize the entire Myers–Perry differential geometry in Lean. Third, formalize the **Klein-four symmetry algebra** used by the project: both involutions, their commutation, the four group elements, and their action on parameter/sector labels. Fourth, formalize the **diagonal-sector parity consequence**: after the exchange involution fixes the sector, the spectral set is invariant under `\delta\mapsto-\delta`. Do not automatically claim that every individually labelled branch is even unless you also assume/prove local branch uniqueness. Fifth, formalize the easy exact combinatorics/algebra: `a=s+\delta`, `b=s-\delta`, `a^2-b^2=4s\delta`, hence the spheroidicity vanishes on `s=0` or `\delta=0`; the `l+1` multiplet count; and any other exact finite identities currently pinned only by Python/SymPy tests. If you want the “quadratic termination” statement in Lean, first weaken it to a precise theorem: an even `C^2`/analytic function has vanishing first derivative at zero and its first nonzero odd-free local term can be quadratic. A geometric exceptional-line termination theorem needs extra nondegeneracy assumptions and should not be formalized with stronger wording than the mathematics supports. 
12. **Then create one science-freeze commit.** At that point you want one clean commit where full CI is green, all slow tests pass, all dangerous candidates pass convergence/adversarial checks, all authoritative JSONs point to that commit, the withdrawn diagnostics have disappeared from active claims, the adaptive boundary search is finished, and you have decided whether hyperboloidal/quasiresonant work is inside or explicitly outside the paper. Tag that commit something like `science-freeze-v1`. **Only then** generate figures from the frozen JSONs. The manuscript should consume those exact frozen artifacts rather than creating or editing scientific numbers by hand.